#@file
# Contains the mappings for fields in Fedora => Spotlight

---
full_title_field:
  ds: DCA-META
  element: { field: "title" }

full_image: Advanced.jpg
thumb: Thumbnail.png

streams:
  DCA-META:
    elems:
      - { field: description }
      - { field: creator }
      - { field: publisher }
      - { field: subject }

